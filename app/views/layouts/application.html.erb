<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link rel="icon" href="/favicon.png" type="image/png">
 
    <script type="text/javascript">
      window.RAILS_ENV = '<%= Rails.env %>'
    </script>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= vite_client_tag %>
    <%= vite_javascript_tag 'browser' %>

    <%= inertia_headers %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
